---
interface Crumb {
  label: string;
  href?: string;
}
interface Props {
  items: Crumb[];
  class?: string;
}
const { items, class: cls = '' } = Astro.props;
---
<nav aria-label="Breadcrumb" class:list={['text-small', cls]}>
  <ol class="flex flex-wrap items-center gap-2 text-ink-500">
    {
      items.map((c, i) => (
        <li class="flex items-center gap-2">
          {c.href && i < items.length - 1 ? (
            <a class="transition-colors hover:text-brand-700" href={c.href}>{c.label}</a>
          ) : (
            <span aria-current="page" class="font-medium text-ink-700">{c.label}</span>
          )}
          {i < items.length - 1 && <span class="text-line" aria-hidden="true">/</span>}
        </li>
      ))
    }
  </ol>
</nav>
