---
import Base from '../layouts/Base.astro';
import ContactBody from '../components/sections/ContactBody.astro';
import { hvacBusinessLd, breadcrumbLd } from '../lib/schema';
import { localizedPath } from '../i18n/utils';

const locale = 'en' as const;
const title = 'Contact Aerocomfort | Free AC Quote — Call or WhatsApp';
const description =
  'Get a free quote for AC installation, repair or maintenance on the Costa Blanca. Call, WhatsApp, or send a quick request — we reply fast.';
const crumbs = [
  { name: 'Home', url: localizedPath('/', locale) },
  { name: 'Contact', url: localizedPath('/contact', locale) },
];
---
<Base
  locale={locale}
  path="/contact"
  title={title}
  description={description}
  jsonLd={[hvacBusinessLd(), breadcrumbLd(crumbs)]}
>
  <ContactBody locale={locale} />
</Base>
