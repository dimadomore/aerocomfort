# Dependencies
node_modules/

# Build output
dist/
.astro/

# Wrangler / Cloudflare local state & cache (D1 local db, etc.)
.wrangler/

# Environment / secrets
.env
.env.*
!.env.example
.dev.vars

# Local lead "table" (mini-CRM) written by the dev server — test data, not committed
data/leads.csv
data/leads.ndjson

# OS / editor
.DS_Store
Thumbs.db
*.log

# Local agent state
.claude/settings.local.json

# Obsidian vault settings
.obsidian/

# Skill working state / caches
src/components/.impeccable/
